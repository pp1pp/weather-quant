services:
  weather-quant:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file:
      - config/.env
    environment:
      - DASHBOARD_PASSWORD=${DASHBOARD_PASSWORD:-}
    restart: unless-stopped
